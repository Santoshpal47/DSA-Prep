#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> arr;
    // int size;
    // cin >> size;

    // arr.resize(size);

    // int i = 0;
    // while (i < size) {
    //     cin >> arr[i];
    //     i++;
    // }

    // i = 0;
    // while (i < size) {
    //     cout << arr[i] << " ";
    //     i++;
    // }
    // cout << endl;
    // auto it = lower_bound(arr.begin(), arr.end(), 4);
    // cout<<*it<<endl;
    // cout<< it-arr.begin();
    // cout<< it-arr.begin();

    // int a, b;
    // cin >> a;
    // cin >> b;
    // cout << "value of a: " << a << endl
    //      << "value of b: " << b << endl;
    // a = a + b - (b = a);
    // cout << "value of a: " << a << endl
    //      << "value of b: " << b;

    // int num = 456;
    // int lol=num;
    // int cnt = 0;
    // while (num)
    // {
    //     cnt++;
    //     num /= 10;
    // }
    // cout << "value of num: " << lol << endl
    //      << "no. of digits :" << cnt << endl;


    // int digit=log10(lol)+1;
    // cout << "value of num: " << lol << endl
    //      << "no. of digits :" << digit << endl;

    string str="abcd";
    str.erase(str.begin()+1);
    cout<<str;

    return 0;
}
